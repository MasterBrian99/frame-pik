export enum STATUS {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  DELETED = 'DELETED',
}

export enum COLLABORATOR_ROLE {
  OWNER = 'OWNER',
  USER = 'USER',
}

export enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}
